server:
  port: 8083
  servlet:
    context-path: /ecommerce-management/api/v1/
spring:
  application:
    name: ecommerce-cart-ms
application:
  session:
    host-url: ${SESSION_URL:http://localhost:8085}
  item:
    host-url: ${ITEM_URL:http://localhost:8082}